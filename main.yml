---
- name: Setting up server
  hosts: vulcan
  become: no

  pre_tasks:
    - name: Update and upgrade repositories.
      apt:
        update_cache=yes
        upgrade=yes
      become: yes

  roles:
    - geerlingguy.docker
    - docker-compose
    - haste-server
    # - vim
    # - samba
    # - duplicati
    # - haste-client
    # - gitea
    # - qbittorrent
    # - calibre-web
    # - jellyfin
